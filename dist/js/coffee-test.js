(function() {
    var obj, square;

    square = function(x) {
        return x * x;
    };

    $('.account').attr({
        "class": 'active'
    });

    log(object["class"]);

    obj = {
        a: 'b',
        c: 'c',
        d: {
            e: 'f',
            g: 'h',
            i: function() {
                return console.log('hello world');
            }
        }
    };

}).call(this);